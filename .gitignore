# Move / Sui build output (regenerated by `sui move build`/`sui move test`)
build/

# Sui CLI local state
.sui/

# Python (legacy Python helpers, if any)
__pycache__/
*.py[cod]
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Local env (keep .env.example committed)
.env
.env.local
.env.*.local
!.env.example
!.env.local.example
!.env.local-stub

# Per-project lockfiles are committed by their owners; do not ignore Move.lock / package-lock.json / pnpm-lock.yaml.
